@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <div class="float-right">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Add @Model</a>
    </div>
}
